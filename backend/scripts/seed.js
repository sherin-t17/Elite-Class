require('dotenv').config();

console.log('Seed script disabled. This project now starts without demo data.');
console.log('Use `node scripts/resetData.js` if you want to wipe the current database.');
